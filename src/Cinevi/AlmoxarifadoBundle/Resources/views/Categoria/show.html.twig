{% extends 'CineviAdminBundle:Crud:base-show.html.twig' %}

{% set title = item.nome %}
{% set routeList = path('cget_categorias-reservaveis') %}
{% set routeEdit = path('edit_categorias-reservaveis', { 'id': item.id }) %}

{% block dtdd %}
    {% include 'CineviAlmoxarifadoBundle:Categoria:item.html.twig' %}

    <h2 id="reservaveis" class="h3"><a href="#reservaveis">Reserváveis</a></h2>

    <div class="table-responsive">
        <table class="table table-striped table-hover">
            <thead>
                <tr>
                    {% include 'CineviAlmoxarifadoBundle:Equipamento:list-thead.html.twig' %}
                </tr>
            </thead>
            <tbody>
                {% for item in pagination %}
                    <tr>
                        {% include 'CineviAlmoxarifadoBundle:Equipamento:list-tbody.html.twig' %}
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>

    {% block blockPagination %}
        {% include 'CineviAdminBundle:Pagination:pagination.html.twig' with { name: 'numResultados' } %}
    {% endblock %}

{% endblock %}
