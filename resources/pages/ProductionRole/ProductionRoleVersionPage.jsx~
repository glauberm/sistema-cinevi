import React from 'react';

import SecureLayout from '../../layouts/SecureLayout';
import ProductionRoleCreateOrUpdateForm from '../../forms/ProductionRole/ProductionRoleCreateOrUpdateForm';

export default function ProductionRoleRevisionPage(props) {
    const { id } = props.match.params;

    return (
        <SecureLayout title="Versão de grupo de trabalho">
            <div className="box">
                <ProductionRoleCreateOrUpdateForm id={id} showRequest="revision" />
            </div>
        </SecureLayout>
    );
}
