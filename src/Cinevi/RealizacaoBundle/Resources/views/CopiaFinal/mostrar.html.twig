{% extends 'CineviAdminBundle:Crud:base-mostrar.html.twig' %}

{% set title = item.nome %}
{% set routeAtual = path('get_copiaFinal', { 'id': item.id }) %}
{% set routeListar = path('get_copiaFinals') %}
{% set routeEditar = path('edit_copiaFinal', { 'id': item.id }) %}
{% set routeAdicionar = path('new_copiaFinal') %}

{% block maisActionsButton %}
    {% if is_granted('ROLE_FUNCIONARIO') %}
        {{ parent() }}
    {% endif %}
{% endblock %}

{% block maisBotoes %}
    {% if is_granted('ROLE_FUNCIONARIO') %}
        {{ parent() }}
    {% endif %}
{% endblock %}

{% block dtdd %}

    {% include 'CineviRealizacaoBundle:CopiaFinal:item.html.twig' with {item: item.direcao} %}

{% endblock %}
