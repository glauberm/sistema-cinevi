<div class="row">
    <div class="col-xs-12">
        <dt>{{ item.funcao.nome }}</dt>
        {% for user in item.users %}
            <dd><a href="{{ path('user_user_show', { 'params' : user.id }) }}">{{ user.username }}</a></dd>
        {% endfor %}
    </div>
</div>
