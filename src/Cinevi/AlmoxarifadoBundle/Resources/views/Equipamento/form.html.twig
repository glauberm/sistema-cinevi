{{ form_row(form.nome) }}
<div class="row">
    <div class="col-sm-6">{{ form_row(form.patrimonio) }}</div>
    <div class="col-sm-6">{{ form_row(form.categoria) }}</div>
</div>
{{ form_row(form.obs) }}
<div class="row">
    <div class="col-sm-4">{{ form_row(form.manutencao) }}</div>
    <div class="col-sm-4">{{ form_row(form.uso) }}</div>
    <div class="col-sm-4">{{ form_row(form.consumivel) }}</div>
</div>
