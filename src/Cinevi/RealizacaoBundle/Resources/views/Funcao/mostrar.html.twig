{% extends 'CineviAdminBundle:Crud:base-mostrar.html.twig' %}

{% set title = item.nome %}
{% set routeListar = path('get_funcaos') %}
{% set routeEditar = path('edit_funcao', { 'id': item.id }) %}

{% block maisActionsButton %}
    {% if is_granted('ROLE_DEPARTAMENTO') %}
        {{ parent() }}
    {% endif %}
{% endblock %}

{% block maisBotoes %}
    {% if is_granted('ROLE_DEPARTAMENTO') %}
        {{ parent() }}
    {% endif %}
{% endblock %}

{% block dtdd %}

    {% include 'CineviRealizacaoBundle:Funcao:item.html.twig' with {item: item} %}

{% endblock %}
