{% extends 'CineviAdminBundle:Crud:base-listar.html.twig' %}

{% set title = 'Projetos' %}
{% set routeAdicionar = path('new_projeto') %}

{% block thead %}
    <th>{{ knp_pagination_sortable(pagination, 'Nome', 'item.nome') }}</th>
{% endblock %}

{% block tbody %}
    <td>
        <a href="{{ path('get_projeto', { 'id': item.id }) }}">{{ item.nome }}</a>
    </td>
{% endblock %}
